"""Projection layer — transform canonical profile into configured output."""

# TODO: Load runtime output config (field selection, rename, provenance, confidence).
# TODO: Project CanonicalProfile to dict/JSON without mutating the canonical model.
